{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command') && echo \"$cmd\" | grep -q 'git commit' && git push -u origin HEAD 2>&1 || true"
          }
        ]
      }
    ]
  }
}
